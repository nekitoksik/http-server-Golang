# User Service API

RESTful API для управления пользователями, заданиями и реферальной системой. Сервис разработан на Go с использованием Clean Architecture и включает JWT-аутентификацию.

## 🛠 Технологии

### Backend
- **Go 1.25** — основной язык программирования
- **Gin** — веб-фреймворк для HTTP API
- **GORM** — ORM для работы с PostgreSQL
- **JWT (golang-jwt/jwt)** — аутентификация через JSON Web Tokens
- **bcrypt** — хеширование паролей
- **golang-migrate** — управление миграциями БД
- **Swagger (swaggo)** — автоматическая документация API

### Infrastructure
- **PostgreSQL 15** — реляционная база данных
- **Docker & Docker Compose** — контейнеризация приложения

### Architecture
- **Clean Architecture** — разделение на слои (handlers, services, repositories, domain)
- **Dependency Injection** — внедрение зависимостей вручную
- **DTO Pattern** — разделение domain моделей и API моделей

---
## 🚀 Установка и запуск

### Требования

- Go 1.25
- Docker и Docker Compose

### Существующие проблемы/возможные улучшения
- Файл .env лежит в корне проекта. Я специально не стал добавлять его в .gitignore, чтобы для проверки тестового не требовалось тратить время на его создание
- Нет тестов, ни unit, ни e2e, ни integration. Можно было бы реализовать, но для такого уровня тестового решил протестить все вручную
- DTO используются не везде, где хотелось бы.
- Не реализованы ручки для работы с Task, чтобы можно было выполнять CRUD операции над ними
- Отсутствует логирование запросов, нет структурированного вида, есть лишь стандартный logger gin.


- Так же можно добавить интерфейсы для всех сервисов, чтобы их mock-ать
